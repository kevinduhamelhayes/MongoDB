use("sample_training")
db.trips.find({ 
    $expr: {
        $eq:["$start station id", "$end station id"]
    }
 }).count()

 db.trips.find({ 
    $expr: {
        $eq:["$start station id", "$end station id"]
    }
 }).count()
 